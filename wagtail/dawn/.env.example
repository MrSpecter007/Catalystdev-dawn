# Django
DJANGO_SECRET_KEY=change-me-to-a-long-random-string
DJANGO_SETTINGS_MODULE=dawn.settings.production

# Hosts — use IP for now, swap for domain later
ALLOWED_HOSTS=187.127.252.86
WAGTAILADMIN_BASE_URL=http://187.127.252.86

# SSL — set to true only after certbot is configured
HTTPS=false

# Database (MySQL)
DB_NAME=dawn_db
DB_USER=dawn_user
DB_PASSWORD=change-me
DB_HOST=localhost
DB_PORT=3306

# Email (optional — fill in for contact forms / password resets)
EMAIL_HOST=smtp.yourmailprovider.com
EMAIL_PORT=587
EMAIL_HOST_USER=you@yourdomain.com
EMAIL_HOST_PASSWORD=change-me
DEFAULT_FROM_EMAIL=you@yourdomain.com
